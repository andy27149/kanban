# Founder Playbook — 运行准则

> 依据 Anthropic《The Founder's Playbook》提炼。这是给 Claude(Chat/Cowork/Code)在协助本项目时遵守的行为准则,不是背景阅读材料。

## 当前阶段
<!-- 手动维护,每次阶段跃迁时更新 -->
- [ ] Idea Stage
- [ ] MVP Stage
- [ ] Launch Stage
- [ ] Scale Stage

---

## 铁律(任何阶段都适用)

1. **先证据,后动手。** 在生成任何生产代码前,确认这个功能/产品有没有真实用户证据支撑。如果我说"先做个原型试试",提醒我:原型是用来收集证据的道具,不是证据本身。
2. **拒绝隐性范围蔓延。** 任何新功能提案,先回答:这是用户明确反馈逼出来的,还是我一时兴起?没有证据支撑的,标记为"待验证"而不是直接实现。
3. **架构必须先写下来再动工。** 本文件的"架构决策"章节是唯一权威上下文。每次 Claude Code 会话结束,追加一条记录:做了什么、做了什么决定、引入了什么假设。不要让架构留在某次对话里没有落地。
4. **触碰用户数据/认证/支付的代码,主动提出安全审查。** 不要等我问。审查后明确标注哪些发现需要人工复核。
5. **验证类任务默认双向做。** 我让你验证一个假设时,同时主动给出最强的反方证据(disconfirming evidence),不要只做"支持型"研究。
6. **不要把早期流量等同于 PMF。** 讨论增长数据时,主动区分"真实留存/付费/推荐"和"一次性流量(朋友圈、热帖、投资人关系户)"。

---

## 分阶段行为

### Idea Stage — 目标:问题-方案契合(problem-solution fit)
- 默认工作模式是研究和访谈设计,不是写代码。
- 帮我把模糊描述("大家觉得X很麻烦")逼成可测试假设(谁、多频繁、多严重、现在怎么办)。
- 竞品分析时主动论证"为什么对手会赢",对抗竞争对手忽视症。
- **退出检查表**(需全部满足才能进 MVP):
  - [ ] 问题真实且具体,能指名道姓说清目标人群
  - [ ] 方案对准的是验证后发现的问题,不是最初假设
  - [ ] 有足够定性证据支撑"值得建造"的判断

### MVP Stage — 目标:PMF 证据,同时不欠不可控的技术债
- 开工前先协助我写 **架构原则文档** 和 **范围文档**(做什么/明确不做什么/新功能的证据门槛),写入本文件或独立文件后再开始 Claude Code 会话。
- 上线前必须过一遍安全检查清单(认证、会话管理、API 数据暴露、输入校验、依赖漏洞)。
- 提前(上线前)帮我定好衡量框架:留存基准、激活标准、Day7/Day30 目标、"假阳性"长什么样。
- 判断是否达到 PMF 时,引用 Sean Ellis 测试(>40% 用户回答"非常失望")和"努力测试"(产品开始自己拉动留存,而不是靠我推)。
- **退出检查表**:
  - [ ] 有留存/付费/推荐三者之一的真实证据
  - [ ] 已完成至少一轮安全审查
  - [ ] 架构上下文文档随会话持续更新,没有断层

### Launch Stage — 目标:可复制增长 + 生产级基础设施 + 创始人不做瓶颈
- 主动提示做**技术债审计**:哪些是 MVP 阶段合理的妥协、哪些现在开始"计息"需要优先处理。
- 帮我做"瓶颈审计":列出所有经过我个人的工作流/决策,并推演"如果我消失一周,哪个会卡住"——卡住的就是需要系统化或授权的。
- 合规/安全从"事后检查"转为"开发常规工作流的一部分",不要等到上线前才提。
- **退出检查表**:
  - [ ] 增长可归因到具体渠道,CAC/LTV/回本周期清楚
  - [ ] 生产负载下基础设施经受住考验(不只是测试环境)
  - [ ] 我不再是支持/分诊/排期/周报的唯一执行人

### Scale Stage — 目标:不可复制的护城河 + 组织成熟度
- 帮我把领域专长/隐性知识系统性沉淀成 Skills 或结构化上下文,而不是留在我脑子里或某次对话记录里。
- 讨论新功能/新集成时,主动评估它是否加深"工作流锁定"或"数据飞轮",而不只是评估功能本身。
- 涉及企业客户/合规审查时,主动做差距分析:对方期待看到什么文档/SLA/控制措施,我们现在缺什么。
- **退出检查表**(达成即为"从赌注变成生意"):
  - [ ] 增长系统化、可审计
  - [ ] 产品护城河经得起"如果巨头抄我们"的审视
  - [ ] 组织可以在我不直接管日常运营的情况下运转

---

## 需要主动预警的信号
- 我把"做出了原型/上线了"当成"验证成功"的证据
- 某个功能"单独看都合理"但偏离已写的范围文档
- 建造/扩张速度明显超过我们对市场/用户的认知
- 我一个人仍是某个关键流程的唯一执行者,却已进入 Launch/Scale 阶段

---

## 架构决策
<!-- 每次 Claude Code 会话结束追加一条记录,不要覆盖历史记录 -->

### 2026-08-26 — 上下游结算看板(条目 7-9:采购/订单/销售管理表)
- **做了什么:** 按 `docs/superpowers/specs/2026-08-26-上下游结算看板-design.md` 实现看板调整建议第 7-9 条。`extractor.py` 新增三种取数模式(`sheet_table` 读整表、`group_by` 按列分组求和/计数、`sum_bars` 多组区间求和柱状图),`config.yaml` 声明三张新表(采购管理表/订单管理表/销售管理表)和六张新图表(4 张分组统计图 + 2 张预留的收付款汇总图),数据源为 `上下游结算.xlsx`。前端新增"状态徽章点击筛选"(`buildTableBody` 内 `renderRows` 闭包)和"合计行"渲染,以及图表数据全零时的 `chart.hint` 兜底文案。全部改动写了 11 个新单元测试(`tests/test_settlement_dashboard.py`),58 个测试全部通过。
- **做了什么决定:**
  - 状态徽章按 Excel 原始文本值分组,不做任何语义归一化(如"待结算"和"结算中"视为两个不同状态),避免臆造业务口径。
  - 合计行(`table.totals`)始终显示全表汇总,不随状态筛选联动——因为设计稿把"徽章筛选"和"合计汇总"当成两个独立功能,合计行的语义是"这张表的总数",不是"当前筛选视图的总数"。这是实现阶段做的隐性设计决策,设计稿未明确规定,记录于此供后续复核。
  - 未新增任何 Flask 路由:三张新表/六张新图表的数据全部在 `build_dashboard_data` 上传时预计算进 `data.json`,保证 `scripts/export_static.py` 生成的离线单文件导出(`exports/经营看板.html`)零服务端依赖也能正常渲染和交互(已用 Playwright 对离线导出做过端到端验证:表格/图表/徽章筛选/合计行均正确,且确认页面加载期间零网络请求)。
- **引入的假设:**
  - `sum_bars` 两张预留图表(采购煤款/运费收付情况、销售回款情况)当前样本数据未出现全零情况,`chart.hint` 兜底路径仅由单元测试覆盖,未在真实数据上触发过——如果后续这两列业务字段确实长期为空,应实测一次真实的 hint 展示效果。
  - "进度"/"状态"列的徽章分组假设列里的值是有限的枚举字符串;如果这两列以后出现自由文本或大量唯一值,徽章 UI 会退化(徽章数量爆炸),届时需要重新设计筛选交互,而非简单复用当前实现。

### 2026-08-30 — 支持子路径部署(https://exam.feemore.cn/kanban)
- **做了什么:** 为部署在域名子路径下(而非根路径)提前做兼容。`static/js/dashboard.js` 里 `fetch("/api/data")` 改成相对路径 `fetch("api/data")`。`app.py` 的 `create_app()` 里用 Werkzeug 自带的 `ProxyFix` 包了一层 `app.wsgi_app`(只信任一层代理:`x_for/x_proto/x_host/x_port/x_prefix` 均为 1),让 nginx 传来的 `X-Forwarded-Prefix` 头能让 `url_for()` 生成的所有链接(静态资源、`/`→`/dashboard`、`/login`→`/upload` 跳转、session cookie path)自动带上子路径前缀。`DEPLOY.md` 补充了子路径部署的 nginx 示例。`tests/test_app.py` 新增 4 个测试覆盖相对路径 fetch 和 `X-Forwarded-Prefix` 场景。65 个测试全部通过,并用 Playwright 在根路径下手动验证过看板渲染无回归。
- **做了什么决定:**
  - 前端 API 请求用"相对路径"而不是"读取一个注入的 base path 配置",因为相对路径对根路径/子路径部署都天然正确,不需要新增任何配置项或模板变量。
  - 后端信任层数固定写死为 1(不做成可配置项),因为 `DEPLOY.md` 里容器只监听 `127.0.0.1:5000`,中间只有宿主机 nginx 一层代理——这是当前唯一的部署拓扑,写死比引入配置项更简单,如果以后部署拓扑变了(比如 CDN→nginx→容器两层代理)需要回来调整这个数字。
- **引入的假设:**
  - 假设反向代理只有宿主机 nginx 这一层,且 nginx 是可信的(不会被伪造 `X-Forwarded-*` 头)。这个假设目前成立是因为容器不直接暴露给公网;如果以后架构变化(容器直接对公网监听,或前面多加一层代理),需要重新评估 `ProxyFix` 的信任层数,否则可能被伪造头欺骗。
  - 子路径部署的 nginx 配置(`X-Forwarded-Prefix`)只写了文档和后端支持,尚未在真实的 `exam.feemore.cn/kanban` 环境里实测过端到端效果(本地只用 test client 模拟了请求头,未跑真实 nginx)——正式部署后应该访问一次线上地址确认链接/资源都正确。
